services:
  web-api:
    build: ./src/.
    container_name: notification-service
    restart: always
    environment:
      - APP_NAME=Notification_Service
    expose:
      - 8080

  nginx:
    build: ./nginx/.
    container_name: nginx
    restart: always