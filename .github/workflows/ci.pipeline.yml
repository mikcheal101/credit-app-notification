name: CI pipeline

# Trigger deployment only when pushed to the main branch
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
      - uses: aws-actions/setup-sam@v2
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2
      - run: sam build --use-container
      - run: sam deploy --no-confirm-changeset --no-fail-on-empty-changeset
  
  # build:
  #   name: This builds the docker image from the source code
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v3
  #   - name: Login to Dockerhub
  #     env:
  #       DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  #       DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD }}
  #     run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD

  #   - name: Build Docker image
  #     run: docker build -t ${{ secrets.DOCKER_USERNAME }}/credit-app-notification ./src

  #   - name: Push to Dockerhub
  #     run: docker push ${{ secrets.DOCKER_USERNAME }}/credit-app-notification:latest

